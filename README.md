# React Shop - Full (Fake API)

This is a ready-to-run React shopping frontend that uses DummyJSON for product data and simulates CRUD operations on the client.

Steps to run:

```
npm install
npm run dev
```

Open the dev URL shown by Vite.
